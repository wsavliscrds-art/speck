// IMPORTANTE: no celular, "localhost" é o próprio celular — não o seu PC.
// Coloque aqui o IP da máquina onde o backend está rodando (ex.: 192.168.0.10),
// ambos na MESMA rede Wi-Fi. Descubra com `ipconfig` (Windows) ou `ifconfig`/`ip a` (Mac/Linux).
export const API_BASE = 'http://192.168.0.10:4000';
